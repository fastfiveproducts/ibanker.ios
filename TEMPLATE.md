Template v0.5.0
https://github.com/fastfiveproducts/template.ios
Adopted from template.ios v0.5.0 @ 02eb27d33 (the "Release v0.5.0" main marker) on 2026-09-04, reconciled 0.4.6 -> 0.5.0 (`b004240dc` -> `02eb27d33`, spanning 0.4.7, 0.4.8, 0.4.9, and 0.5.0 in one pull) by issue #62 — the iBanker template leg of the fleet v0.5.0 consumer round (rides milestone 2.0.2; the #61 tablet leg builds on it). This file records where iBanker IS, not a target.

Reconcile note: iBanker is local-only (no Firebase), so most of the span's delta is N/A — the account-data-service seam (#181), content sources (#182/#203/#210), auth-ready gate (#227), overlay accessibility/exit work (#232/#247), nav-style config (#193), and the posts/Messages exemplar (#239) all live in subsystems iBanker does not carry (those files were never present; nothing to prune). What actually reached iBanker: the #179 absolute-pointer license-header sweep (all template-derived files; NO header lines advanced, per the boilerplate-sweep exemption); #236's @MainActor on ActivityLogEntry.testObjects; #238's .contentWidthCapped in the wholesale ActivityLogView; #225's AppIdentityFooterView (adopted — it was generalized FROM iBanker's bespoke Settings footer, which is now RETIRED in favor of the component + the two new AppConfig members); the #180/#213 ScreenshotMode merge-file shape (launchArgument "-ibScreenshotScreen" preserved under the MARK); #171's .gitignore fix (both pin-hiding rules removed; iBanker has no SPM deps, so no new pins to commit); and tools header modernization (add-test-target.rb, validate-screenshots.swift — the latter byte-identical to the template). Adopted template test artifacts: AppIdentityFooterViewTests + ContentWidthTests, plus the app-only LayoutBreakpointTests (pins the 840 tier the template pins via LaunchViewNavigationTests, which iBanker cannot adopt — the "what the template cannot pin, adopters owe" convention); suite 48 -> 56.

Recorded divergences (the register that keeps the next reconcile cheap):
- Architecture (standing): folder naming (Model/, ModelSupport/, View/, Store/ vs the template's Models/, Views/Main|System/, Repositories/); the app entry point (iBankerApp launches straight to MainTabView — no Firebase configure, LaunchView, or overlay stack); a local Tab enum in MainTabView standing in for NavigationItem; app-original files use the lighter header (no Template/contact lines). Test tree mirrors iBanker's OWN folder names (adopted template tests live at iBankerTests/View/ and iBankerTests/ViewSupport/), so template-test compare rows read `new`/`app-only` by design.
- ViewSupport/ViewHelpers.swift is adopted PRUNED (new at this reconcile, interim): the template file's four OverlayState/OverlayAnimation extensions are omitted because their types live in the overlay subsystem iBanker does not carry. Template-side fix filed FIRST: template.ios#263 (move the overlay extensions into the omittable overlay subsystem). Retire this pruning at the first reconcile after #263 lands; until then a wholesale re-copy of ViewHelpers will NOT compile here.
- tools/generate-screenshots.sh remains iBanker's #55 single-class original (with tools/make-screenshot-seed.swift, the app-only persistence-seeding route). Deferred deliberately: the 0.5.0 CHANGELOG stages re-adoption of the template's two-class tooling "whenever you add an iPad screenshot set" — that is the #61 tablet leg / 2.0.2 store leg (read template.ios#256 before any iPad set). ScreenshotMode.swift itself IS converged; its launchArgument must stay identical to the launch argument the script passes — iBanker's #55 script hardcodes the `-ibScreenshotScreen` literal at three sites (no LAUNCH_ARG variable exists until the template script is re-adopted).
- Deliberately NOT arrived at this reconcile (staged, not missed): the #237 adaptive shells (LaunchView-coupled seam; iBanker's shell adaptation is #61's chartered work), #238 caps in app-owned bodies (per the CHANGELOG: children apply them in their own bodies — #61), #218 orientation posture (project-file surface; #61, and see ibanker.ios#39 for the INFOPLIST trap), the ti#222 trailing-slash URL flip (app+store together at the next submission), RELEASE.md adoption + listing-doc leveling vs the #194 skeleton + the #221 PASTE-READY audit (the 2.0.2 store leg).
- Retired at this reconcile: the bespoke Settings aboutSection (-> AppIdentityFooterView); ActivityLogView's trailing Spacer + historical no-Clear-All-Logs comment (the former #28 divergence's convergence note now lives HERE: #167 removed the button upstream in v0.4.4, the two agree); unrecorded comment-only drift in ActivityLogEntry, ActivityLogView, and CustomLabeledContentStyle (found by content-diffing against the 0.4.6 marker, converged by the wholesale takes).

Note: this file is the single source of truth for the app's adopted template version. Per-file "Template vX (updated)" headers are NOT advanced by an upgrade — they track each file's template.ios counterpart version (which advances only when that file changes); after this reconcile the newest counterpart headers are v0.5.0 (ActivityLogView, ViewHelpers, AppConfig, SettingsView), with ActivityLogEntry at v0.4.9 and ScreenshotMode at v0.4.8. iBanker gained this file in issue #52 (2026-07-30, the fleet retrofit; Android counterpart ibanker.android#10).
Prior: Template v0.4.6 @ b004240 (adopted 2026-07-31, issue #51).
