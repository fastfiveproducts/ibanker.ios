Template v0.4.6
https://github.com/fastfiveproducts/template.ios
Adopted from template.ios v0.4.6 @ b004240 (the "Release v0.4.6" main marker) on 2026-07-31, reconciled 0.4.3 -> 0.4.6 (`3ff3a65a3` -> `b004240`, spanning both 0.4.4 and 0.4.6; 0.4.5 was an Android-only release, skipped on the iOS ledger) by issue #51 — the iBanker leg of the fleet iOS testing wave (kickoff template.ios#174). This file records where iBanker IS, not a target.
Reconcile note: iBanker is local-only (no Firebase), so most of the 0.4.3->0.4.6 delta is N/A — the account/cloud/posts/contact layers, the launch/overlay choreography, the #169 HomeView/MainTabView brand-timing (iBanker shows its brand via the toolbar, not an OverlayManager fade), the #165 SettingsView grouped-Form frame (iBanker's SettingsView already renders a grouped Form via its own app-specific body), and the #164 .multiControlRow helper (iBanker uses .borderless). What actually reached iBanker: #167 removed "Clear All Logs" upstream (ActivityLogView, converging the former #28 divergence -> Template v0.4.4); the #166 licenses@...llc -> .com header sweep was already reflected. The iOS test stack (add-test-target.rb recipe + Swift Testing conventions) was adopted as iBanker's own reverse-ported suite (iBankerTests, 48 cases), not copied from the template's account-app tests.
Note: this file is the single source of truth for the app's adopted template version. Per-file "Template vX (updated)" headers are NOT advanced by an upgrade — they track each file's template.ios counterpart version (which advances only when that file changes); iBanker's newest counterpart header is v0.4.4 (ActivityLogView.swift, after the #167 convergence). iBanker gained this file in issue #52 (2026-07-30, the fleet retrofit; Android counterpart ibanker.android#10); before that the adopted version was recorded only inline in AGENTS.md.
